Console.WriteLine("How old are you?");// para imprimir - equivale console.log JS
$ dotnet run pra rodar no terminal
  string input = Console.ReadLine();//The word input represents a variable 

dotnet new console// para abrir novo arquivo de projeto

qual a diferença entre     Console.WriteLine("How old are you?"); Console.ReadLine();